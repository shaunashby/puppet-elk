#
output {
        elasticsearch {
                host     => "<%= @logstash_es_hostname %>"
                port     => "9200"
                cluster  => "<%= @logstash_es_cluster_name %>"
                index    => "<%= @logstash_es_index_format %>"
                protocol => "http"
        }
}
